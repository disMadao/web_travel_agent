# ==========================================
# AI 智能旅行助手 - 环境变量配置模板
# ==========================================
# 
# 📋 使用说明：
# 1. 复制此文件并重命名为 .env
# 2. 填写你自己的 API Key 和配置
# 3. 不要将 .env 文件上传到 Git 或分享给他人
#
# ==========================================

# ==========================================
# Supabase 配置（数据库和认证）
# ==========================================
# 获取地址: https://supabase.com/dashboard
# 1. 创建项目
# 2. 在 Settings -> API 中找到：
#    - Project URL (SUPABASE_URL)
#    - service_role key (SUPABASE_KEY) ⚠️ 注意：使用 service_role，不是 anon key
# 3. 在 SQL Editor 中执行 database/supabase_setup.sql 初始化数据库

SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_supabase_service_role_key

# ==========================================
# DeepSeek AI 配置（智能行程规划）
# ==========================================
# 获取地址: https://platform.deepseek.com
# 1. 注册账号
# 2. 在 API Keys 页面创建新的 API Key
# 3. 充值一定金额（建议 50-100 元，实际消耗很少）

OPENAI_API_KEY=your_deepseek_api_key
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-chat

# ==========================================
# 高德地图配置（地理编码、POI 搜索）
# ==========================================
# 获取地址: https://console.amap.com/dev/key/app
# 1. 注册账号并实名认证
# 2. 创建应用
# 3. 添加 Key，选择"Web服务"类型
# 注意：这是服务端 Key，不是 Web 端 Key

AMAP_API_KEY=your_amap_web_service_key

# ==========================================
# 应用配置
# ==========================================
# 环境: development / production
APP_ENV=production

# 安全密钥（用于 JWT 签名）
# ⚠️ 重要：请修改为随机字符串，至少 32 位
# 生成方法：在 Python 中运行 `import secrets; print(secrets.token_urlsafe(32))`
SECRET_KEY=your_secret_key_change_this_in_production

# API 前缀
API_PREFIX=/api/v1

# CORS 允许的来源（JSON 数组格式）
CORS_ORIGINS_STR=["http://localhost:3000", "http://localhost:5173"]

# ==========================================
# 可选：更换 AI 模型
# ==========================================
# 如果要使用 OpenAI GPT 而不是 DeepSeek：
# OPENAI_API_KEY=your_openai_api_key
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4

# 如果要使用阿里云通义千问：
# OPENAI_API_KEY=your_dashscope_api_key
# OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# OPENAI_MODEL=qwen-plus

# ==========================================
# 💡 提示
# ==========================================
# - 所有带 "your_" 前缀的值都需要替换
# - API Key 要妥善保管，不要泄露
# - 生产环境务必修改 SECRET_KEY
# - 定期更新 API Key 以保证安全
# ==========================================

